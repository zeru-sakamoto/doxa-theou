# Copy this file to `.env` and point these at your local source Bible DB.
# Used only by scripts/import_bible.py to build bible.sqlite. Relative paths
# are resolved from the repo root.
BIBLE_SOURCE_DB=db/your-source.db
BIBLE_SOURCE_TABLE=your_table_name
