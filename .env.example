# Copy this file to `.env` and point this at your local source Bible DB.
# Used only by scripts/import_bible.py to build bible.sqlite. Relative paths
# are resolved from the repo root. Source DB has fixed table names
# (`verses`, `headings`) — see DESIGN.md.
BIBLE_SOURCE_DB=db/your-source.db
